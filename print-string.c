#include "main.h"

int print_str(char *string)
{
	int i = 1;
	if (!string)
	{
		print_str("(null)");
		return;
	}
	else 
	{xhile (*string)
		{
			_putch(*string)
			string++;
			i++;
		}
	}
	return (i);
}
